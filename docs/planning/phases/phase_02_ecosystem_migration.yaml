---
id: documentation-standard-phase-02-ecosystem-migration
title: "Phase 02: Ecosystem-Wide UDS Migration"
description: "Migrate all 14 NizamIQ repositories to full UDS compliance: scaffold missing docs directories, copy canonical governance files, update CONTEXT.md files, and validate all frontmatter."
last_audited: "2026-02-21"
status: PENDING
authoritative_source: docs/planning/phases/phase_02_ecosystem_migration.yaml
version: 1.1.0
tags: ["documentation-standard", "uds", "ecosystem-migration", "compliance", "frontmatter"]
---

phase_id: "02"
name: "Ecosystem_Wide_UDS_Migration"
description: >
  Migrate all 14 NizamIQ repositories to full UDS compliance: scaffold missing docs
  directories, copy canonical governance files, update CONTEXT.md files, and validate
  all frontmatter. This phase brings the entire ecosystem to a consistent documentation
  standard, enabling reliable AI agent navigation across all repos.
dependencies: ["01"]
status: PENDING
steps:
  - id: "02.1"
    action: "Scaffold missing docs/architecture, docs/api, docs/governance directories in all repos missing them"
    logic_order: 1
    status: PENDING
    verification_method: "for repo in Fireside KubeClaw nizamiq.com zitadel-config; do ls repos/$repo/docs/architecture repos/$repo/docs/api repos/$repo/docs/governance 2>/dev/null && echo OK || echo MISSING; done"
    proof_of_work: ""
    observation: "Fireside, KubeClaw, nizamiq.com, and zitadel-config are missing some docs subdirectories."

  - id: "02.2"
    action: "Copy canonical AI_AGENT_INSTRUCTIONS.md and ci_cd_standards.md into docs/governance/ for all repos missing them"
    logic_order: 2
    status: PENDING
    verification_method: "for repo in gateway-config zitadel-config; do test -f repos/$repo/docs/governance/AI_AGENT_INSTRUCTIONS.md && echo OK || echo MISSING; done"
    proof_of_work: ""
    observation: "gateway-config and zitadel-config are missing governance files."

  - id: "02.3"
    action: "Update CONTEXT.md in all repos to ensure all four required sections are present (System Architecture, Dependency Map, CI/CD, Scope)"
    logic_order: 3
    status: PENDING
    verification_method: "for repo in $(ls repos/); do grep -l 'CI/CD' repos/$repo/CONTEXT.md 2>/dev/null && echo OK || echo MISSING; done"
    proof_of_work: ""
    observation: "Several repos added after Protocol 5 are missing the CI/CD section in CONTEXT.md."

  - id: "02.4"
    action: "Add UDS frontmatter to all markdown files in docs/ that are missing it"
    logic_order: 4
    status: PENDING
    verification_method: "python3 scripts/frontmatter_audit.py --all-repos"
    proof_of_work: ""
    observation: "Ongoing: new files added by Protocol 5 PRs triggered UDS lint failures. Automated fix script exists at /tmp/fix_preflight_fm.py."

  - id: "02.5"
    action: "Add AGENTS.md to all repos missing it (zitadel-config)"
    logic_order: 5
    status: PENDING
    verification_method: "test -f repos/zitadel-config/docs/planning/AGENTS.md && echo EXISTS"
    proof_of_work: ""
    observation: "zitadel-config was added after the initial AGENTS.md rollout."

  - id: "02.6"
    action: "Add .github/workflows/nizamiq_standard.yml to zitadel-config"
    logic_order: 6
    status: PENDING
    verification_method: "test -f repos/zitadel-config/.github/workflows/nizamiq_standard.yml && echo EXISTS"
    proof_of_work: ""
    observation: "zitadel-config has no CI workflows. The standard doc-lint workflow must be added."

quality_controls:
  - control: "All 14 repos pass UDS lint on main"
    check_command: "CI validates on push to each repo"
    result: PENDING
  - control: "All CONTEXT.md files have four required sections"
    check_command: "python3 scripts/context_audit.py --all-repos"
    result: PENDING
